# This file was auto-generated by Fern from our API Definition.

"""
jsonable_encoder converts a Python object to a JSON-friendly dict
(e.g. datetimes to strings, Pydantic models to dicts).

Taken from FastAPI, and made a bit simpler
https://github.com/tiangolo/fastapi/blob/master/fastapi/encoders.py
"""

import base64
import dataclasses
import datetime as dt
from enum import Enum
from pathlib import PurePath
from types import GeneratorType
from typing import Any, Callable, Dict, List, Optional, Set, Union

import pydantic
from .datetime_utils import serialize_datetime
from .pydantic_utilities import (
    IS_PYDANTIC_V2,
    encode_by_type,
    to_jsonable_with_fallback,
)

SetIntStr = Set[Union[int, str]]
DictIntStrAny = Dict[Union[int, str], Any]


def jsonable_encoder(obj: Any, custom_encoder: Optional[Dict[Any, Callable[[Any], Any]]] = None) -> Any:
    custom_encoder = custom_encoder or {}
    t_obj = type(obj)
    # Fast-path for custom encoder (type-based mapping)
    if custom_encoder:
        if t_obj in custom_encoder:
            return custom_encoder[t_obj](obj)
        else:
            for encoder_type, encoder_instance in custom_encoder.items():
                if isinstance(obj, encoder_type):
                    return encoder_instance(obj)
    if isinstance(obj, pydantic.BaseModel):
        encoder = getattr(
            obj.model_config if IS_PYDANTIC_V2 else obj.__config__,
            "json_encoders", {}
        )  # type: ignore
        # Merge encoder dict if custom_encoder is present
        if custom_encoder:
            encoder = {**encoder, **custom_encoder}
        obj_dict = obj.dict(by_alias=True)
        root_keys = ("__root__", "root")
        for key in root_keys:
            if key in obj_dict:
                obj_dict = obj_dict[key]
        return jsonable_encoder(obj_dict, custom_encoder=encoder)
    if dataclasses.is_dataclass(obj):
        return jsonable_encoder(dataclasses.asdict(obj), custom_encoder=custom_encoder)
    # Common primitives check (bytes, Enum, Path, str/int/float/None, dt)
    if isinstance(obj, (str, int, float, type(None))):
        return obj
    if isinstance(obj, bytes):
        return base64.b64encode(obj).decode("utf-8")
    if isinstance(obj, Enum):
        return obj.value
    if isinstance(obj, PurePath):
        return str(obj)
    if isinstance(obj, dt.datetime):
        return serialize_datetime(obj)
    if isinstance(obj, dt.date):
        return str(obj)
    if isinstance(obj, dict):
        return {jsonable_encoder(k, custom_encoder=custom_encoder): jsonable_encoder(v, custom_encoder=custom_encoder) for k, v in obj.items()}
    # Sequence encoding (list, set, tuple, frozenset, generator)
    if isinstance(obj, (list, set, frozenset, GeneratorType, tuple)):
        return [jsonable_encoder(item, custom_encoder=custom_encoder) for item in obj]
    # Fallback serializer

    def fallback_serializer(o: Any) -> Any:
        attempt_encode = encode_by_type(o)
        if attempt_encode is not None:
            return attempt_encode

        try:
            data = dict(o)
        except Exception as e:
            errors: List[Exception] = [e]
            try:
                data = vars(o)
            except Exception as e2:
                errors.append(e2)
                raise ValueError(errors) from e2
        return jsonable_encoder(data, custom_encoder=custom_encoder)

    return to_jsonable_with_fallback(obj, fallback_serializer)
